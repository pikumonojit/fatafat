import { createClient } from '@supabase/supabase-js'

export const dynamic = 'force-dynamic'
export const revalidate = 0
export const metadata = { title: 'Today Result – Matka King Fatafat', description: 'View the latest published Matka King Fatafat result information for today. Results are provided for informational purposes only.' }

const supabase = createClient('https://pselksqspkulqkfrnioi.supabase.co', process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)
const times = ['10:30 AM','12:00 PM','01:30 PM','03:00 PM','04:30 PM','06:00 PM','07:30 PM','09:00 PM']
function todayIndia(){ return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata'}).format(new Date()) }
function dateText(d){ return new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Kolkata',day:'2-digit',month:'short',year:'numeric'}).format(new Date(`${d}T00:00:00+05:30`)) }
function single(v){const s=String(v||'').replace(/\D/g,'');return s.length===3?String(s.split('').reduce((a,x)=>a+Number(x),0)%10):'—'}
export default async function TodayResult(){const date=todayIndia(); const {data}=await supabase.from('results').select('round_number,result').eq('result_date',date).eq('status','published').order('round_number'); const rows=Array(8).fill(null); (data||[]).forEach(r=>rows[r.round_number-1]=r); const count=new Date(`${date}T00:00:00+05:30`).getDay()===0?4:8; return <main className="page"><header className="topbar"><div className="top-inner"><a className="logo" href="/"><span className="crown">♛</span><span><b>MATKA KING <em>FATAFAT</em></b><small>FAST | CLEAR | INFORMATION</small></span></a><nav><a href="/">Home</a><a className="active" href="/today-result">Today Result</a><a href="/old-results">Old Result</a><a href="/patti-list">Patti List</a></nav></div></header><section className="panel today-panel" style={{margin:'28px auto',maxWidth:1100}}><div className="panel-title"><span>▣ &nbsp; TODAY'S RESULT</span><b>{dateText(date)}</b></div><div className="classic-result"><div className="classic-date">{dateText(date)} <span className="today-badge">TODAY</span></div><div className="classic-head">{rows.slice(0,count).map((_,i)=><div key={i}>{i+1}</div>)}</div><div className="classic-numbers">{rows.slice(0,count).map((r,i)=><div key={i}>{r?.result||'—'}</div>)}</div><div className="classic-single">{rows.slice(0,count).map((r,i)=><div key={i}>{single(r?.result)}</div>)}</div><div className="classic-times">{rows.slice(0,count).map((_,i)=><div key={i}>{times[i]}</div>)}</div></div><div className="notice-strip">Results are published for information only. No betting, wagering, deposits or payment services are offered.</div></section><footer><p><a href="/">Matka King Fatafat</a> · <a href="/old-results">Old Results</a> · <a href="/patti-list">Patti List</a></p></footer></main>}
